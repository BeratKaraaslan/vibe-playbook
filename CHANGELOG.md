# CHANGELOG — Vibe-Coding Orchestration Playbook

## v8.2 — 2026-07-11
- **New `/adapt` command (both profiles) — Phase 0 derives the project-specific architecture (user request).** The gap: Phase 0 produced the docs but not the project-specific `.claude/` configuration (the origin project's hand-made `fortune-domain-expert` was this gap filled manually). `/adapt` runs at the END of Phase 0 (its proposal rides the Phase 0 gate — no new gate) and re-runs when a new domain surface appears mid-project. Its checklist: domain-expert agent(s) with a consistent skeleton (advisory, read-only tools, untrusted-content rule) · **verifier invariants** (the "fill during Phase 0" placeholder must not survive Phase 0 empty — closes the second review's STATE-07) · CLAUDE.md fields (one-liner, base branch, commands, Scale & pragmatism) · the stack's REAL settings allow-list · optional doc classes (spine / prompts / design / go-live) · MCP suggestions for the human. Guardrails: the §12 minimal-set rule is embedded ("every agent is carried maintenance" — items approved one by one) and **control-plane honesty** (in solo, settings/commands are deny-protected → changes are presented as paste-ready blocks; a deny is never worked around).
- Design note: shipped as a **versioned command, not a pasted kickoff prompt** — the same lesson as v3 (materialize > prose): pasted prompts drift per project; a shipped command is one keystroke, identical everywhere, and §15-versioned. Wired into both STARTGUIDE Phase 0 kickoffs (step 6/5) + §3. Package `8.2.0`.

## v8.1 — 2026-07-10
> **The second external-review intake** (a full code/security/architecture review of 8.0.0), processed per the §15 protocol. Every headline claim was reproduced locally before action (the reviewer's own extraction artifact — a "typo" that was actually the 🚦 emoji — was the one REJECTED bug claim). The deepest point was accepted whole: our language claimed more than the implementation delivers; v8.1 makes the words match the mechanism instead of trying to become a security product.
- **CONFIRMED + FIXED — CLI safety (the worst finding):** same-profile `--force` silently reset living docs (a custom PRD reverted to the template — reproduced). **`--force` is REMOVED entirely**: the CLI never overwrites an existing file; re-scaffolding means deleting files yourself, knowingly. Also fixed: symlinked `target`/`target/.claude` refused (files landed outside the target — reproduced); literal `gitignore` collision now checked alongside `.gitignore`; strict argument validation; honest file-count message. Removing `--force` also closes the v7-overlay/stale-commands path wholesale.
- **CONFIRMED + FIXED — main-guard:** compound commands containing a commit are blocked on protected branches (TOCTOU: `echo … && git add && git commit` passed — reproduced; the guard cannot see files created later in the same call, so the commit must be standalone) · **untracked files now count** as code changes · **`.claude/` removed from the docs allowlist** (a weakened `main-guard.sh` committed as "docs-only" — reproduced; control-plane ≠ docs) · `git -C <path>` now checks the TARGET repo, not the hook's cwd · quote-stripping + comment-stripping fix both tokenizer failure modes (quoted branch false-block, marker-in-comment false-pass) · **fail-closed when python3 is missing** (both guards).
- **CONFIRMED + FIXED — guard-env:** multi-suffix forms (`.env.local.backup`, `.env.production.enc`) now blocked · mixed Grep globs (`{.env,.env.example}`) no longer laundered by the allowed token (with a lookahead so `.environment.ts` stays free — caught by our own regression suite mid-fix).
- **ADOPTED — "state lives on disk" honored at GATE 2:** the approved plan is now WRITTEN INTO the spec ("## Approved plan (GATE 2)" section; implementer excerpts come from there, not chat); status enum gains `gate2-approved`; the combined-profile contradiction resolved (`/gate1` handles spec+plan as one approval and skips `/gate2`).
- **ADOPTED — human-timing made technical:** all 13 commands now carry `disable-model-invocation: true` — the model can never auto-fire a gate/side-effect command; gates run on human timing only, exactly as the methodology claims.
- **ADOPTED — solo control-plane deny:** `Edit/Write` denied on `.claude/settings.json`, `settings.local.json`, `hooks/**`, `commands/**` — the agent can no longer rewrite its own guards mid-session (agents/** stays writable on purpose: it is the advisory plane, verified by the human at GATE 4, and Phase 0 fills the verifier checklist there). `.gate4-ok` stays writable — it is a documented drift-guard, not an auth record.
- **ADOPTED — honest failure + honest language:** pre-compact now writes under `$CLAUDE_PROJECT_DIR`, reports write failures with non-zero exit (it faked success — reproduced), and its stale comment is fixed · README/PLAYBOOK/hook headers drop "physically blocks ANY access" for "accident guard, not a security boundary — pair with branch protection, clean-runner CI, Claude Code sandboxing" · STARTGUIDEs gain a **high-assurance** bullet (branch protection · verifier tests in clean CI: a reviewed branch's test code is that branch's code · sandboxing pointer) · agents gain an **untrusted-repository-content** rule (prompt-injection posture) · feedback file gains a **redaction** step before handback · base branch single-sourced (CLAUDE.md field; gate4/verifier reference `<base>`) · exact-version pin wording (`@8.1.0`, not `@8`) · "v1 → v6" layout line fixed · secret-scan learns Slack/Google/Stripe shapes · CI: `permissions: contents: read` + no-force/collision guard smokes · `.claude/settings.local.json` gitignored (Claude Code creates it; untracked counting must not flag it).
- **REJECTED (recorded, per protocol):** signed/SHA-bound approval records + single-use JSON markers (the marker is a drift-guard by design; real assurance = PR review + branch protection, already the documented path) · a 12-state transition-validated state machine (ceremony; `gate2-approved` + on-disk plan covers the real gap) · shipping sandbox profiles by default (pointer documented instead; sandbox config is Claude Code-version-specific) · CLI staging/atomic-rollback/migrate/uninstall machinery (no-force + collision refusal shrinks the blast radius to near zero) · skills-dir migration (commands supported; `disable-model-invocation` adopted from the new surface) · multi-OS/Node CI matrix + fuzz suites + provenance/governance pack (P2 for a single-maintainer project) · dropping the verifier's opus pin (field-proven; configurability documented).
- Tests: main-guard 16→**22**, guard-env 30→**34** per template — every fixed bug has a regression case. Package `8.1.0`.

## v8 — 2026-07-10
> **The first external-review intake** — a third-party package review processed per the §15 intake protocol: every claim verified before action; verdicts recorded below. Reviewer's four concrete bug claims all REPRODUCED before fixing.
- **CONFIRMED + FIXED (hooks, both templates; regression-tested — suites grew 28→30 and 8→16):**
  - **prefix-branch bug:** a `.gate4-ok` containing `wip/P-1` approved a `wip/P-10` merge (substring match) → exact-token match now.
  - **`git -C . commit` bypass:** flag forms slipped past main-guard → flag-tolerant matcher (`git -C …`, `git -c k=v …`), with `git log --grep commit` still false-positive-free.
  - **symlink bypass:** a differently-named symlink to `.env` passed guard-env's path check → `realpath` resolution on tool path keys (Bash-side symlinks remain out of string-matching reach — documented limit).
  - **`git cherry-pick` gap:** landed code on main without the merge ritual → blocked outright on protected branches.
  - **hardcoded main/master:** `trunk`-style branches were unprotected → `VIBE_PROTECTED_BRANCHES` env / one-line edit.
- **ADOPTED — command renames (built-in shadowing was real; reproduced /review collision):** `/spec`→`/gate1` · `/plan`→`/gate2` · `/review`→`/gate4` · `/checkpoint`→`/wip`. The reviewer proposed a `vp-` prefix; **adapted** instead to the gate-uniform scheme — collision-proof AND the command name states which gate you are at. All docs swept.
- **ADOPTED — packaging:** `test/` now ships in the tarball (`npm test` works for consumers — a broken script was published in 7.1.0; turning it into "verify the hooks on your own machine" is a feature) · **GitHub Actions CI** (hook suites + CLI smoke incl. the mix guard + pack sanity) · **profile stamp** `.claude/.vibe-playbook` (profile+version provenance) + **profile-mix guard**: `init` refuses to overlay a different profile even with `--force` (the reviewer produced a solo/orchestrated hybrid; switching stays the documented manual 3-file swap).
- **ADOPTED — docs honesty:** README/STARTGUIDEs gained: workspace-trust note (project permissions apply only after interactive first run) · Windows→WSL · version-pinning tip + "no in-place upgrade by design" · "few-hour prototype? skip the playbook" · verifier-model configurability note · protected-branches note.
- **REJECTED (with reasons, per protocol):** narrowing solo permissions (the wide-permission design stands — gates+hooks are the control points; the trade is documented and the reviewer's own text acknowledges the audience) · upgrade/migrate/uninstall/dry-run machinery (ceremony; the pinned-version model is deliberate — stamp+guard added instead) · migrating commands to skills/plugins (commands are supported; revisit if deprecated) · unpinning the verifier from opus (field-proven; made configurable-in-docs instead).
- Package `8.0.0`; templates re-stamped "← playbook v8".

## v7.1 — 2026-07-10
- **The backflow artifact (user request): both templates ship `PLAYBOOK-FEEDBACK.md`** — an append-only, **methodology-only** experience log (friction · theater gates · bypasses+why · hook misfires · context-hygiene failures · **model-shift notes** — models improve; the workflow must keep up). Sessions write entries the moment they observe something; retro PLAYBOOK-labeled answers land there. At project end the human hands the ONE file back to the canonical repo — backflow no longer depends on end-of-project archaeology (the v7 harvest required manually excavating the origin project).
- **Self-carrying intake protocol:** the file's own header instructs its future processor — entries are **claims, not truth** (verify → anecdote-mark), filter through optimum>perfect, judge against the three first-class goals (updates that weaken consistency/sustainability/context do not pass), verdict per entry adopt/adapt/**reject** (rejections recorded with the why), never bulk-apply. The same protocol is recorded in §15 on the canonical side.
- Wiring: workflow.md doc-class line + retro pointer (both) · CLAUDE.md doc map (both) · memory-seeds (write-the-moment-you-observe; solo seed gained its missing retro line) · STARTGUIDE daily flow + project-end handback step (both). Package `7.1.0`.

## v7 — 2026-07-10
> **The first real §15 backflow:** lessons harvested from the origin project's docs/workflow (a live product built on playbook v2 — 2 phases shipped, prod live). Filter applied: only mechanisms that REDUCE the human load / preserve context made it in; nothing that adds ceremony. Deliberately NOT adopted: per-command `allowed-tools` restrictions (brittle), hook `statusMessage` cosmetics, template files for optional doc classes (empty files in every project = noise — the classes are defined in the manifest and materialize in Phase 0 when needed).
- **Design track finally has real mechanics (§13 + `template/docs/design/README.md` + `_TEMPLATE-brief.md`):** the field-proven loop — brief → Claude Design prototype (READ-ONLY repo, no commits) → Export/Handoff bundle → Code implements (tokens only) → **guardian gate on the Code side only** (discovered constraint: **Claude Design cannot read `.claude/agents/`** — Design-side conformance comes from the published Design System) → PR → fixed-format report to the Manager → STATUS ledger. Two field lessons recorded: `DESIGN-CONTEXT.md` lives **inside the web-app directory** (Design's read-only access can see it — shared as-built truth) · **front-load hybrid** (global shell up-front; module screens JIT after their spec locks — earlier = rework).
- **Two optional doc classes in the manifest (§7 + both workflows):** `shared-spine.md` — multi-module products: the cross-part CONTRACT; specs then define **only deltas** (the origin project keeps 14 module specs small this way); spec template gained the delta line · `prompts/` — LLM products: **runtime asset, not a living doc** (versioned `.vN`, agent-maintained, every output stores promptVersion+model) + the timing rule: **draft the base persona BEFORE the first LLM part** (re-toning debt otherwise).
- **The PR-flow blind spot, closed honestly (§9 + both workflows):** when merges happen on GitHub, **main-guard never sees the merge** — the GATE 4 marker = the PR approval and the invariant moves to **branch protection**; the hook keeps guarding local commits.
- **Script > runbook-prose (§5 + both workflows):** a runbook step that runs twice becomes an executable script (`ops/` — the origin project's `backup.sh`/`restore-test.sh`); the runbook documents the WHY and calls it. The ops counterpart of "hook > instruction".
- **Sham-green CI + go-live checklist (§6 + both workflows):** a green pipeline is not proof — the field case: three prod blockers that gates+CI missed were caught by a human walking the real journey on staging; a short go-live checklist (env · migrations · seeds · secrets · full-journey smoke) is now recommended before the first cutover.
- **Anti-gold-plating calibration (both CLAUDE.md):** new "Scale & pragmatism" section — one honest line about scale + explicit NON-goals, read every session (the origin project's "side-project scale; gold-plating NO" note proved its worth).
- **Bloat budget refined (§7 + both workflows + progress template):** the budget is **content, not line count** (field finding: a status board under the line budget with a 4 kB single-paragraph "Now" cell). progress.md gained a "Now" section capped at 3–5 bullets.
- **Verifier upgrades (both templates):** `model: opus` pinned (GATE 4 is quality-critical and rare — field-proven) + a "project checklist (fill during Phase 0)" placeholder for domain invariants (e.g. idempotent debits, charge-on-success). Commands gained `argument-hint` frontmatter; settings gained `$schema`, a `Bash(printenv:*)` deny, and explicit `.env.example` write allowance (orchestrated).
- Package `7.0.0`; templates re-stamped "← playbook v7".

## v6.1 — 2026-07-08
- **The audience axis made explicit (user request):** the two profiles were shipped framed by project shape (parallel vs serial); the intended split was by audience — **orchestrated = software developers** (read specs/diffs, coordinate sessions), **solo = vibe coders** and developers in vibe mode (the agent drives all git/terminal mechanics; the human steers in plain language). Now stated at every decision surface: README profile table ("Made for" row + rule of thumb), CLI `--help` profile descriptions, both STARTGUIDE headers, the §17 table, the npm package description.
- **Solo GATE 4 is plain-language-first:** `/review` now presents (a) a plain summary — what the part does, what the verifier confirmed, remaining risks, what to try by hand — then (b) a technical appendix (file:line spots + diff command) for those who read code.
- **The honest line, written down (§17 + solo STARTGUIDE + README):** solo removes the *mechanical* developer dependencies, not the *judgment* one — gates still ask a human "is this what I wanted / does it work?"; that judgment is the safety backbone, especially around money/auth.
- Package `6.1.0` (minor: framing + one command ergonomic; the methodology core is unchanged — major stays = playbook v6).

## v6 — 2026-07-08
- **The canonical repo is fully English:** PLAYBOOK.md + CHANGELOG.md translated (README already was). The v4-era decision to keep the playbook Turkish as the "human-rationale layer" is reversed by user request — the repo is no longer mixed-language. Chat language remains free (rule 11 unchanged); everything written is English.
- **npm packaging:** the repo now ships as the **`vibe-playbook`** package (name verified free on the registry). `package.json` (version 6.0.0 — **package major = playbook version**), MIT LICENSE, zero-dependency **`bin/cli.js`**: `npx vibe-playbook init <solo|orchestrated> [dir]` copies the profile, restores `.gitignore`, chmods the hooks, refuses to overwrite existing files (`--force` to override), prints next steps.
- **npm tarball workaround:** npm always strips files named `.gitignore` from packages — template gitignores are stored as **`gitignore`** (no dot); the CLI renames on init, and the manual-copy instructions in STARTGUIDEs/README gained an `mv gitignore .gitignore` line. `npm pack --dry-run` verified the tarball contains all template files (including `.claude/` and `.env.example`).
- **Tests moved into the repo:** the hook suites (previously scratchpad-only) now live in `test/` with repo-relative paths; `npm test` runs them against BOTH templates (guard-env 28 scenarios ×2 + main-guard 8 ×2 — all passing).
- Templates re-stamped "← playbook v6"; README gained the npm quick start and dropped the "playbook is Turkish" note.

## v5 — 2026-07-07
- **New profile: `template-solo/` (34 files) — single-session vibe (user request).** The main session = the orchestrator; product code is neither written nor read in the main context — three subagents carry it: **scout** (read-only code Q&A, compact findings + file:line) · **implementer** (one part step: code + tests + checkpoint commit, compact report) · **verifier** (GATE 4, same file as orchestrated). New commands: **/part** (drives a part through spec→gates→impl→verify→merge, stopping at every 🚦) · **/tidy** (the context-hygiene ritual: sync docs to ground truth + rotation + sweep chat-only decisions → declare the reset options). **PreCompact is default-ON in solo.** No phase-kickoffs.md and no /new-part — the kickoff/handover concepts are meaningless in solo; instead the **restart test**: after `/clear`, resume from CLAUDE.md + docs + memory (memory survives `/clear`). The honest mechanism: the model cannot wipe its own context — prevention (delegation-first, rule 12) + safe reset (/tidy, rule 13; the human runs /compact–/clear) + the safety net (PreCompact). The §4.3 boundary is deliberately inverted in solo: development goes to subagents, the user watches compact reports; ground-truth verification lives on as the post-dispatch git check.
- **New §17:** the two-profile comparison table + solo architecture + the three context-hygiene layers + **profile switching** (living-docs are identical in both → mid-project solo↔orchestrated switching is supported; start solo, switch when parallelism appears). §14 marked as the "orchestrated profile"; the header gained the v5 profile line.
- **Shared-DNA byte parity:** 4 hooks + verifier + the spec/plan/checkpoint/gate3 commands + doc skeletons are exact copies from `template/` (verified by diff — two profiles derived from one source, no fork drift).
- **README.md (English):** the two-profile comparison + setup and daily usage + hook/command reference + versioning; the repo's front door. `template/` version markers bumped to v5 (with the orchestrated-profile label).
- **Solo autonomy (user request):** (a) **wide permissions** — Edit/Write + `git commit/merge` in allow (push/reset/rebase ask, destructive ops deny); the human is never stopped between gates — the control points are the GATES, not permission prompts; the invariants live in hooks; (b) **batched dispatches** — a small part = the whole approved plan in one implementer dispatch, bigger = 2–4-step packages; a checkpoint commit per step (granular history, few interruptions); (c) **the `/clear` recommendation at part boundaries** — the solo equivalent of orchestrated's fresh session; /tidy now declares the reset options. The honest limit spelled out: the model cannot reset its own context (/clear–/compact are user commands); what it can do is prevention + making the reset zero-risk + a one-keystroke offer; quality order: boundary-/clear > /compact > auto-compact.
- **guard-env hardened + verified (user request, both templates):** closed bypasses — globs (`cat .env*`, `.env.*`), no-space redirects (`>.env`), case variants (`cat .ENV` — the macOS FS is case-insensitive), the Grep tool's `glob` parameter, subdirectory paths (`api/.env`); the `source/cp/mv/base64/python-open/variable-assignment` paths were already caught, now proven by tests. Only `.env.example/sample/template` are fully accessible. **The block-behavior rule (CLAUDE.md 3):** on a guard-env block STOP — NO retries via another path, report to the user + wait for approval; env values are always placed by the human. The settings deny list expanded to `**/.env*` variants (6 names, subdirectories included). **A 28-scenario suite passes 28/28 on both templates** (18 bypasses blocked, 10 legitimate uses stay free — process.env / the dotenv package / .environment.ts with zero false positives); the older 18-test suite passes unchanged.

## v4 — 2026-07-04
> Theme: "optimum > perfect" — mechanisms that reduce the human load were added; ones that add ceremony deliberately were not.
- **§9 leak protocol + the secret-scan hook (CLAUDE.md rule 3 extended):** if the user pastes a sensitive value into chat (DB URL, API key…), the model does not repeat it / does not write it into any file-doc-command; if it landed anywhere it deletes it immediately; it notifies + **recommends rotation** (the honest limit: chat history cannot be truly deleted — the only permanent fix is rotation). New **UserPromptSubmit hook `secret-scan.sh`**: detects common shapes (userinfo conn-strings · sk-/AKIA/ghp_/github_pat_ · PRIVATE KEY · JWT · key=value) and reminds the protocol at exactly that moment — without blocking (false-positive cost ~zero).
- **§9 context discipline (new CLAUDE.md rule 9):** a mid-phase user question gets the SHORT answer requested — no re-dumping the whole phase flow with every question; the full summary only when asked or at a gate.
- **§3 + §9 working-tree isolation (new CLAUDE.md rule 10):** parallel sessions (client/backend etc.) cannot share one working tree — the root of conflicts/code loss. One active session per directory; parallel parts in **separate git worktrees**; the Manager partitions parts with **disjoint file scopes** (overlapping ones run sequentially); a parallelism check added to `/new-part`; checkpoint commit before branch switches.
- **§9 the language rule (new CLAUDE.md rule 11) + templates fully English:** chat follows the user (Turkish fine); ALL documentation (living-docs, specs, runbooks, commit messages, code comments) in ENGLISH — rationale: fewer tokens · stronger instruction-following · consistent terminology across sessions. The template's 32 files (doc skeletons, command/agent prompts, hook comments+messages) translated; the mechanics (file names, regexes, the `.gate4-ok` path, markers) unchanged; hook tests re-run (18/18 + secret-scan 3/3). Terminology: KAPI→GATE, parça→part, devir→handover; the design numbering G-N kept (GATE is never abbreviated to a single letter — no collision).
- **④ Design-track tooling:** the design decision-maker is **Claude Design** (connects to Claude Code via MCP); the connection requires Claude Code **from the terminal (CLI)** — recorded in §1/§13 + the template (workflow, STARTGUIDE, design STATUS).

## v3 — 2026-07-04
- **`template/` materialized (the biggest change):** the §14 skeleton is no longer prose but real files — a LEAN CLAUDE.md (8 critical rules) + workflow.md (**the normative summary**: rules only, rationale in the playbook — the kernel/rationale split emerged without restructuring the playbook) + STARTGUIDE (setup + a ready Phase 0 kickoff) + living-doc starters + the spec template (with a gate-profile field) + a runbook template + `.claude/` (settings + 3 hooks + the verifier agent + 6 commands) + `memory-seed/`. The bootstrap-drift source is closed: no more re-generation from prose — copy & use.
- **§9 main-guard + /gate3 (applying "hook > instruction"):** "nothing enters main without GATE 4" is now physical — code commits on main and unmarked merges are blocked (docs-only commits stay free; the marker = `.claude/.gate4-ok` after human approval). The `/gate3` command produces mechanical evidence for GATE 3 (test/lint/typecheck). The principle: the right hook removes a human vigil, it does not add ceremony. *(Both hooks passed an 18-scenario test suite.)*
- **§2 per-part gate profile:** for a small/low-risk part GATES 1+2 combine into one approval (a proactive answer to gate fatigue; the §15 retro calibration remains as the reactive layer); never on money/auth/data-loss surfaces. The profile is written into a spec field.
- **New §2.1 cancel/rollback:** part cancellation (branch abandoned + a one-line record) · plan falls apart mid-IMPL → delta-plan back to GATE 2 · GATE 4 finds a spec flaw → back to GATE 1.
- **§9 anti-confabulation generalized:** not only Ops — every session that has been through compaction says "not in the docs → not recorded".
- **§10 the handover test (the Manager symmetric of the Ops test):** information in the handover prompt that is missing from the docs = a docs gap; the doc is fixed, not the prompt.
- **§11 memory mechanism note:** memory is project-directory-scoped and role-blind; role selectivity comes from the kickoff. The seed file moved to `memory-seed/` (the first Manager session saves it).
- **④ Design decision-maker = Claude Design** (MCP; terminal mandatory) recorded in §1/§13 + template.
- *Deliberately not added:* a two-document restructuring of the playbook (maintenance burden; the template already is the normative form) · an S/M/L part taxonomy (judgment suffices — the follower is a developer) · a stateless Manager (human friction; the long-lived Manager stays as cache, freshness is tested at handover) · extra ceremony hooks.

## v2 — 2026-07-03
- **§1/§5 (Ops):** the "persistence" rationale fixed — the source is runbooks+infra-state, the session context is only a cache. **Instant-runbook** + **anti-confabulation** rules added (against invented "whys" after compaction: not in the runbook → "not recorded").
- **§2/§4.3 (GATE 4):** verification reads delegated to a **verifier subagent** — the Manager's context stays clean (early handover + stale code memory prevented); the principle: "the Manager's own code memory is also an unreliable source". The boundary defined: dev sessions stay interactive (the user watches/dictates); the subagent handles only bounded, single-report verification tasks.
- **§9/§13 (commits):** "uncommitted-until-review" removed → the **branch + checkpoint commit** model; the invariant "nothing enters main without GATE 4"; squash/curate optional. *(This also closed v1's internal contradiction: §2 said "IMPL (small commits)" while §9 said "NO commits".)*
- **§9:** new working agreement — **"hook > instruction"**: enforceable hygiene rules are applied via harness hooks.
- **§10 (handover):** the trigger clarified = **quality + natural boundary** (not a fixed token count; with a large context, finishing a coherent unit is allowed). "watch /context + let the agent notice" removed (the model cannot introspect its remaining context) → replaced by the **PreCompact hook safety net** — **OPTIONAL**: installed by choice at project start; it does not force handovers, it secures the docs before compaction + notifies.
- **§12/§14:** `pre-compact.sh` (optional) added to the skeleton; the "← playbook vN" fork marker at the top of workflow.md.
- **New §15 (meta-learning):** canonical home · version+changelog · **the phase retro (3 questions:** which gate caught something real / which was approved without reading / which rule was bypassed and why**)** · PROJECT/PLAYBOOK labeling · **gate calibration** (measuring gate fatigue) · backflow to base.

## v1 — before 2026-07-03 (the scratchpad era)
- The first domain-neutral version: 4 session types · human gates (GATES 1–4 + phase gates) · the Manager loop (ground-truth verification, the kickoff skeleton) · the Ops session + the hand-in-hand protocol · the living-docs system (STATE/ARCHIVE, bloat budget) · decision timing · working agreements · tracks · the base-project skeleton.
