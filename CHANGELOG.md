# CHANGELOG — Vibe-Coding Orchestration Playbook

## v5 — 2026-07-07
- **Yeni profil: `template-solo/` (34 dosya) — tek-session vibe (kullanıcı talebi).** Ana session = orkestratör; ürün kodu ana context'te ne yazılır ne okunur — üç subagent taşır: **scout** (salt-okur kod Q&A, kompakt bulgu+file:line) · **implementer** (bir parça-adımı: kod + test + checkpoint commit, kompakt rapor) · **verifier** (GATE 4, aynı dosya). Yeni komutlar: **/part** (parçayı spec→kapılar→impl→verify→merge uçtan uca sürer, her 🚦'da durur) · **/tidy** (context-hijyen ritüeli: docs'u zemine eşitle + rotasyon + chat-only karar süpürme → "SAFE TO COMPACT" ilanı). **PreCompact solo'da default-ON.** phase-kickoffs.md ve /new-part yok — kickoff/devir kavramı solo'da anlamsız; yerine **restart-testi**: `/clear` sonrası CLAUDE.md+docs+memory'den devam edebilme (memory /clear'dan sağ çıkar). Dürüst mekanizma: model kendi context'ini silemez — önleme (delegation-first, kural 12) + güvenli boşaltma (/tidy, kural 13; insan /compact—/clear koşar) + emniyet ağı (PreCompact). §4.3 sınırı solo'da bilinçli tersine döner: geliştirme subagent'a gider, kullanıcı kompakt rapor izler; zemin-doğrulama dispatch-sonrası git kontrolü olarak aynen yaşar.
- **Yeni §17:** iki-profil karşılaştırma tablosu + solo mimarisi + context-hijyen üç katmanı + **mod geçişi** (living-docs iki profilde birebir aynı → proje ortasında solo↔orchestrated geçişi desteklenir; solo başla, paralellik doğunca geç). §14 "orchestrated profil" olarak işaretlendi; başlığa v5 profil satırı eklendi.
- **Ortak DNA byte-parity:** 4 hook + verifier + spec/plan/checkpoint/gate3 komutları + doc iskeletleri `template/`'tan birebir kopya (diff ile doğrulandı — tek kaynaktan türetilmiş iki profil, fork-drift yok).
- **README.md (İngilizce):** iki profilin karşılaştırması + kurulum ve günlük kullanım + hook/komut referansı + sürümleme; repo'nun ön kapısı. `template/` sürüm işaretleri v5'e çekildi (orchestrated profile etiketiyle).
- **Solo otonomi (kullanıcı talebi):** (a) **izinler geniş** — Edit/Write + `git commit/merge` allow'da (push/reset/rebase ask, yıkıcılar deny); kapılar arasında insan hiç durdurulmaz — kontrol noktası izin-promptu değil KAPILAR, invariant'lar hook'ta; (b) **toplu dispatch** — küçük parça = onaylı planın tamamı tek implementer dispatch'i, büyük = 2–4 adımlık paketler; adım başına checkpoint commit (granüler history, az kesinti); (c) **parça sınırında `/clear` önerisi** — orchestrated'daki taze-session'ın solo karşılığı; /tidy artık sıfırlama seçeneklerini ilan eder. Dürüst sınır netleştirildi: model kendi context'ini sıfırlayamaz (/clear—/compact kullanıcı komutları); yapabildiği önleme + sıfır-risk kılma + tek tuşluk öneri; kalite sırası sınırda-/clear > /compact > auto-compact.
- **guard-env sertleştirildi + doğrulandı (kullanıcı talebi, her iki template):** kapatılan bypass'lar — glob (`cat .env*`, `.env.*`), boşluksuz redirect (`>.env`), case (`cat .ENV` — macOS FS case-insensitive), Grep tool'unun `glob` parametresi, alt-dizin path'leri (`api/.env`); `source/cp/mv/base64/python-open/değişken-atama` yolları zaten yakalanıyordu, testle kanıtlandı. Yalnız `.env.example/sample/template` tam erişilebilir. **Blok-davranışı kuralı (CLAUDE.md 3):** guard-env bloklarsa DUR — başka yoldan deneme YOK, kullanıcıya rapor + onay bekle; env değerlerini daima insan koyar. settings deny listesi `**/.env*` varyantlarına genişletildi (6 isim, alt dizinler dahil). **28 senaryoluk test iki template'te de 28/28** (18 bypass bloklandı, 10 meşru kullanım serbest — process.env / dotenv paketi / .environment.ts false-positive'siz); eski 18'lik paket regresyonsuz.

## v4 — 2026-07-04
> İlk saha kullanımından gelen üç iyileştirme (kullanıcı talebi).
- **§9 Sızıntı-protokolü + secret-scan hook (CLAUDE.md kural 3 genişledi):** kullanıcı chat'e hassas değer yapıştırırsa (DB URL, API key…) model değeri tekrar etmez / hiçbir dosyaya-doc'a-komuta yazmaz; yazıldıysa derhal siler; bildirir + **rotasyon önerir** (dürüst sınır: chat geçmişinden gerçek silme yoktur — tek kalıcı çözüm rotasyon). Yeni **UserPromptSubmit hook'u `secret-scan.sh`**: yaygın kalıpları (userinfo'lu conn-string · sk-/AKIA/ghp_/github_pat_ · PRIVATE KEY · JWT · key=değer) sezip protokolü tam o anda hatırlatır — bloklamaz (false-positive maliyeti ~sıfır).
- **§9 Bağlam-disiplini (yeni CLAUDE.md kural 9):** faz ortasındaki kullanıcı sorusuna istenen KISA cevap verilir — her soruda tüm faz akışını/durumu dökme yok; tam özet yalnız istenince ya da kapıda.
- **§3 + §9 Working-tree izolasyonu (yeni CLAUDE.md kural 10):** paralel session'lar (client/backend vb.) tek working tree'yi paylaşamaz — conflict/kod-kaybının kökü. Aynı dizinde tek aktif session; paralel parçalar **ayrı git worktree**'de; yönetici parçaları **dosya-kapsamı ayrık** böler (çakışanlar sıralı); `/new-part`'a paralellik kontrolü eklendi; branch değişimi öncesi checkpoint commit.
- **§9 Dil kuralı (yeni CLAUDE.md kural 11) + template tamamen İngilizce:** chat dili kullanıcıya uyar (Türkçe serbest); TÜM dokümantasyon (living-docs, spec, runbook, commit mesajı, kod yorumu) İNGİLİZCE — gerekçe: daha az token · daha güçlü talimat-takibi · session'lar arası tutarlı terminoloji. Template'in 32 dosyası (doc iskeletleri, command/agent prompt'ları, hook yorum+mesajları) İngilizce'ye çevrildi; mekanik (dosya adları, regex'ler, `.gate4-ok` path'i, ayraçlar) değişmedi; hook testleri yeniden koşuldu (18/18 + secret-scan 3/3). Terminoloji: KAPI→GATE, parça→part, devir→handover; tasarım numaralandırması G-N korundu (GATE tek harfe kısaltılmaz — çakışma yok). PLAYBOOK.md insan-rationale dokümanı olarak Türkçe kaldı.

## v3 — 2026-07-04
> Tema: "optimum > mükemmel" — insan yükünü azaltan mekanizmalar eklendi, tören ekleyenler bilinçli olarak eklenmedi.
- **`template/` materialize edildi (en büyük değişiklik):** §14 iskeleti artık prose değil, gerçek dosyalar — LEAN CLAUDE.md (8 kritik kural) · workflow.md (**normatif özet**: yalnız kural, gerekçe playbook'ta — çekirdek/rationale ayrımı playbook'u yeniden yapılandırmadan kendiliğinden oluştu) · STARTGUIDE (kurulum + Faz 0 kickoff hazır) · living-doc başlangıçları · spec şablonu (kapı-profili alanlı) · runbook şablonu · `.claude/` (settings + 3 hook + verifier agent + 6 command) · `memory-seed/`. Bootstrap-drift kaynağı kapandı: prose'dan yeniden-üretim yok, kopyala-kullan var.
- **§9 main-guard + /gate3 ("hook > talimat"ın uygulanması):** "main'e KAPI 4'süz giriş yok" artık fiziksel — main'de kod-commit ve işaretsiz merge bloklanır (docs-only commit serbest; işaret = insan onayı sonrası `.claude/.gate4-ok`). `/gate3` komutu KAPI 3'e mekanik kanıt bloğu üretir (test/lint/typecheck). İlke: doğru hook insan nöbetini kaldırır, tören eklemez. *(Her iki hook 18 senaryoluk testten geçirildi.)*
- **§2 kapı-profili parça bazında:** küçük/düşük-riskli parçada KAPI 1+2 tek onayda birleşir (kapı-yorgunluğuna baştan önlem; §15 retro-kalibrasyonu reaktif katman olarak kalır); para/auth/veri-kaybı yüzeyinde asla. Profil spec alanına yazılır.
- **Yeni §2.1 iptal/geri-dönüş:** parça iptali (branch terk + tek satır kayıt) · IMPL'de plan çürüdü → KAPI 2'ye delta-plan · KAPI 4 spec kusuru buldu → KAPI 1'e dönüş.
- **§9 anti-confabulation genellendi:** yalnız Ops değil — compaction geçirmiş her session: "docs'ta yoksa kayıtlı değil".
- **§10 devir-testi (Ops testinin yönetici simetriği):** devir-prompt'ta docs'ta olmayan bilgi = docs açığı; prompt değil doc düzeltilir. Sert garanti değil devir-anı sağlık kontrolü (tören eklemez).
- **§11 memory mekanizma-notu:** memory proje-dizini bazlı ve rol-körüdür; rol seçiciliği kickoff'ladır. Seed dosyası `memory-seed/`e taşındı (ilk yönetici session kaydeder).
- **④ Tasarım track tooling:** tasarım karar vericisi **Claude Design** (Claude Code'a MCP ile bağlanır); bağlantı için Claude Code'un **terminalden (CLI)** kullanımı zorunlu — §1/§13 + template (workflow, STARTGUIDE, design STATUS) işlendi.
- *Bilinçli eklenmeyenler:* playbook'un iki-doküman yeniden yapılandırması (bakım yükü; template zaten normatif biçim) · S/M/L parça taksonomisi (yargı yeter, takip eden developer) · stateless yönetici (insan sürtünmesi; uzun-ömürlü yönetici cache olarak kalır, tazelik devirde test edilir) · ek ceremony-hook'ları.

## v2 — 2026-07-03
- **§1/§5 (Ops):** "kalıcılık" gerekçesi düzeltildi — kaynak runbook+infra-state, session context'i yalnız cache. **Anında-runbook** + **anti-confabulation** kuralları eklendi (compaction sonrası uydurma "neden"lere karşı: runbook'ta yoksa "kayıtlı değil").
- **§2/§4.3 (KAPI 4):** doğrulama okumaları **verifier-subagent**'a delege — yönetici context'i temiz kalır (erken devir + bayat kod hafızası önlenir); ilke: "yöneticinin kendi kod hafızası da güvenilmez kaynaktır". Sınır tanımı: dev session'ları interaktif kalır (kullanıcı izler/dikte eder); subagent yalnız sınırlı, tek-raporlu doğrulama işleri.
- **§9/§13 (commit):** "uncommitted-until-review" kaldırıldı → **branch + checkpoint commit** modeli; değişmez kural "main'e KAPI 4'süz giriş yok"; squash/curate isteğe bağlı. *(v1'in iç çelişkisi de kapandı: §2 "IMPL (küçük commit)" derken §9 "commit YOK" diyordu.)*
- **§9:** yeni çalışma anlaşması — **"Hook > talimat"**: enforce edilebilen hijyen kuralı harness hook'uyla uygulanır.
- **§10 (devir):** tetik netleşti = **kalite + doğal sınır** (sabit token sayısı değil; büyük context'te tutarlı birimi bitirmek serbest). "/context izle + agent fark etsin" kaldırıldı (model kendi kalan context'ini içgözlemle bilemez) → yerine **PreCompact hook emniyet ağı** — **OPSİYONEL**: proje başında isteğe göre kurulur; devir dayatmaz, compaction öncesi docs'u güvenceler + bildirir.
- **§12/§14:** iskelete `pre-compact.sh` (opsiyonel) eklendi; `workflow.md` başına "← playbook vN" fork işareti.
- **Yeni §15 (meta-öğrenme):** kanonik ev · versiyon+changelog · **faz-retro (3 soru:** hangi kapı gerçek yakaladı / hangisi okunmadan onaylandı / hangi kural neden bypass edildi**)** · PROJE/PLAYBOOK etiketleme · **kapı kalibrasyonu** (kapı-yorgunluğu ölçümü) · base'e geri akış.

## v1 — 2026-07-03 öncesi (scratchpad dönemi)
- İlk domain-nötr sürüm: 4 session tipi · insan kapıları (KAPI 1–4 + faz kapıları) · yönetici döngüsü (zemin-doğrulama, kickoff iskeleti) · Ops session + el-ele protokol · living-docs sistemi (STATE/ARŞİV, bloat-budget) · karar-zamanlaması · çalışma anlaşmaları · track'ler · base-project iskeleti.
