# .env.example — template. Real values go into .env (never committed; kept out of the agent's reach by the guard-env hook — an accident guard, not a security boundary; see README).
# EXAMPLE_API_KEY=
