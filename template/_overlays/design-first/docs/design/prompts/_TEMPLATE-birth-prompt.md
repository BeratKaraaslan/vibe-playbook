# <app> — birth prompt (opens the Claude Design project)

- **STATUS: DRAFT** *(lifecycle: DRAFT → APPROVED → APPLIED → SUPERSEDED — update this line; never delete the file)*
- **App:** <name> · **Date:** <YYYY-MM-DD>

> The HUMAN pastes everything below the line into a NEW Claude Design chat — sessions never create cloud projects. After reviewing the first build in the UI, the human drops the `<app>-mcp.text` pointer file (project URL + "auth via /design-login"); only then does MCP takeover begin.

--- PASTE BELOW THIS LINE ---

**Project-birth settings:** <project name · design-system link (from D1) · device/viewport · theme (light/dark)>

**Brief (~15 lines MAX — distilled; never the full strategy doc):**
<what the product is · who uses it · tone · the one core flow>

**First package — screens & states:**
<screen: name · route · states (empty/loading/error/success) · flow links between screens>

**DO NOT DESIGN (scope guard):**
<screens/flows outside this package — the tool must not invent them>

**Expected first output:** <what the human reviews in the UI before dropping the pointer file>
